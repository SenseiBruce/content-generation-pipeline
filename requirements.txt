# Capital Architects — YouTube Shorts Pipeline
# Python 3.11+ required

# ── HTTP & Feeds ─────────────────────────────────────────────────────────────
feedparser==6.0.11
requests==2.33.0
python-dotenv==1.2.2

# ── Structured validation ────────────────────────────────────────────────────
pydantic==2.13.4

# ── OpenRouter / LLM ─────────────────────────────────────────────────────────
# (OpenRouter is accessed via pipeline.http_client using the standard OpenAI
#  chat completions endpoint — no special SDK needed)

# ── Runware Image Generation ─────────────────────────────────────────────────
# (Uses direct HTTP requests to the REST API via pipeline.http_client)

# ── Video Stitching ──────────────────────────────────────────────────────────
# Optional fallback if system ffmpeg lacks freetype/drawtext support
pillow==12.3.0

# Voicebox is the default TTS engine (see VOICEBOX_* in .env.template).
# gTTS is optional and lives in requirements-optional.txt.

# ── YouTube Data API v3 ───────────────────────────────────────────────────────
google-api-python-client==2.136.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0

# ── Video / Audio (FFmpeg is a system dependency, not a Python package) ───────
# Install FFmpeg via: brew install ffmpeg  (or apt-get install ffmpeg)
# Voice synthesis uses the Voicebox HTTP API (see VOICEBOX_* in .env.template).
# Optional Coqui TTS extras live in requirements-optional.txt
